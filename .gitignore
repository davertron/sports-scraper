_site
_cache
.env
frontend/node_modules

# root node_modules IS tracked (see package.json "dependencies") -- it's
# vendored so `npm run scrape`/`npm run calendar` never need `npm install`.
# devDependencies (typescript, @types/node) are intentionally NOT vendored;
# run `npm install` locally once if you want `npm run typecheck` to work.